import React from 'react';
import Navigation from '../Navigation/Navigation';
import { AppBarContainer } from './AppBarStyled';

const AppBar = () => {
  return (
    <AppBarContainer>
      <Navigation />
    </AppBarContainer>
  );
};

export default AppBar;
